# Local Test Certificates

Run `./scripts/generate_certs.sh` on macOS / Linux or `scripts\generate_certs.bat` on Windows to generate local CA, Broker, center, and sensor certificates.

Generated files are ignored by Git because they contain local private keys.
